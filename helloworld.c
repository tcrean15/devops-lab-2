#include <stdio.h>

int main() {
    // Use a for loop to iterate from 1 to 10
    for (int i = 1; i <= 10; i++) {
        printf("%d\n", i); // Print the current value of 'i'
    }
    
    return 0;
}
